// init_web.dart - For web platform
// Web platform uses IndexedDB through sqflite_common_ffi_web
void initializeDatabase() {
  // Web uses IndexedDB automatically through sqflite_common_ffi_web
  // No initialization needed as it's handled by the package
}